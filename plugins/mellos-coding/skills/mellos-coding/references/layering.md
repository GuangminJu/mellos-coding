# 分层与组合

适用：模块划分、类与函数职责、依赖关系的设计和审查。

- 分离业务规则、流程编排和技术适配；业务不依赖具体外部实现。
- 依赖单向、无环；优先组合与复用，不以继承复用实现。
- 函数只做一件事，类只有一个变化原因；一起变化的代码放在一起，无关逻辑不混放。
- 不用前置声明或间接层掩盖结构问题；只为实际变化点引入设计模式，避免无用抽象。
- 用最小接口保护不变量，明确资源所有权与生命周期。
- 验证业务行为，不以结构完整或层数多少作为目标。
